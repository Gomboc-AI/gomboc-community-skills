# Audit Approved AMIs, Custom Policy

Here the goal is duplicate, as closely as is relevant, the Sentinel behavior or printing errors.  We use the `audit` remediate command to throw and error that gets captured in the ORL report.

Run `orl remediate ../workspace` to see the behavior.

